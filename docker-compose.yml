version: '3.1'

services:

  db:
    image: postgres
    restart: always
    ports:
      - 5432:5432
    environment:
      - POSTGRES_USER=brunoahcor
      - POSTGRES_PASSWORD=brunoahcor123
      - POSTGRES_DB=dev

  db-adminer:
    image: adminer
    restart: always
    ports:
      - 8090:8080